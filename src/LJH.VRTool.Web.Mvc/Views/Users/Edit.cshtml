@using Abp.Authorization.Users
@model IReadOnlyList<LJH.VRTool.Web.Models.Users.EditUserModalViewModel>
@{
    Layout = "~/Views/Shared/Krpano/_Layout.cshtml";
}
<div class="modal-body">
    <form id="addForm">
        <div class="panel blank-panel">
            <div class="panel-heading">
                <div class="panel-options">
                    <ul class="nav nav-tabs">
                        <li class="active">
                            <a data-toggle="tab" href="tabs_panels.html#tab-4"><i class="fa fa-user"></i>用户信息</a>
                        </li>
                        <li class="">
                            <a data-toggle="tab" href="tabs_panels.html#tab-5"><i class="fa fa-users"></i>角色信息</a>
                        </li>
                    </ul>
                </div>
            </div>
            <div class="panel-body">
                <div class="tab-content">
                    <div id="tab-4" class="tab-pane active">
                        <div class="form-group">
                            <label for="Name">账号</label>
                            <input type="text" name="Name" class="form-control" required maxlength="@AbpUserBase.MaxNameLength">
                        </div>
                        <div class="form-group">
                            <label for="username">名</label>
                            <input class="form-control" type="text" name="UserName" required maxlength="@AbpUserBase.MaxUserNameLength" minlength="2">
                        </div>
                        <div class="form-group">
                            <label for="account">姓</label>
                            <input type="text" name="Surname" class="form-control" required maxlength="@AbpUserBase.MaxSurnameLength">
                        </div>
                        <div class="form-group">
                            <label for="password">邮箱</label>
                            <input type="email" name="EmailAddress" class="form-control" required maxlength="@AbpUserBase.MaxEmailAddressLength">
                        </div>
                        <div class="form-group">
                            <label for="password">密码</label>
                            <input type="password" id="Password" name="Password" class="form-control" required maxlength="@AbpUserBase.MaxPlainPasswordLength">
                        </div>
                        <div class="form-group">
                            <label for="password">是否激活</label>
                            <div class="onoffswitch">
                                <input type="checkbox" checked class="onoffswitch-checkbox" id="IsActive" name="IsActive" value="true">
                                <label class="onoffswitch-label" for="IsActive">
                                    <span class="onoffswitch-inner"></span>
                                    <span class="onoffswitch-switch"></span>
                                </label>
                            </div>
                        </div>
                    </div>
                    <div id="tab-5" class="tab-pane">
                        <div class="form-group">
                            @*@foreach (var item in Model)
                            {

                                <label class="checkbox-inline i-checks" for="RoleNames_@item.Id">
                                    <input type="checkbox" value="@item.Name" name="RoleNames" id="RoleNames_@item.Id">@item.DisplayName
                                </label>
                            }*@
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </form>
</div>
<script src="~/lib/krpano/hplus/js/jquery.min.js?v=2.1.4"></script>
<script src="~/lib/krpano/hplus/js/plugins/iCheck/icheck.min.js"></script>
<script>
    $(document).ready(function () {
        $('.i-checks').iCheck({
            checkboxClass: 'icheckbox_square-green',
        });
    });
</script>
<script src="~/lib/jquery/dist/jquery.js"></script>
<script>
    function save(){
        var formData = $("#addForm").serializeArray();
        $.ajax({
          url: "@Url.Action("Edit", "Users")",
          type: "POST",
          data: formData,
          dataType: "json",
          success: function (res) {
              if (res.result.status == "ok") {
                  layer.msg('编辑成功!', { icon: 1, time: 3000 });
                  parent.location.reload();//刷新父窗口
                  var index = parent.layer.getFrameIndex(window.name);
                  parent.layer.close(index); //再执行关闭
              }
              else {
                  layer.msg('编辑失败!', { icon: 2, time: 5000 });
              }
          },
          error: function (e) {
              layer.msg('请求出错!', { icon: 3, time: 5000 });
          }
      });
    }
</script>
